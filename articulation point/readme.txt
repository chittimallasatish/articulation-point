readme file

to execute the program we first need the latest java software  installed in the system and make sure the environtment variable are set correct

open terminal and enter "javac filename.java" and then enter the java "filename"
the inputs are automatically taken from the text file which i have created
it then gives outputs based on the inputs in the text file , if u want to experiment then u can change the values or add more values to the existing and save the file and execute the program again
